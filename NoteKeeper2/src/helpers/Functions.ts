export * as Functions from "../helpers/Functions"

export function creteNote(title: string, content: string, bgColor: string, pinned: boolean){
    const noteDiv = document.querySelector("#noteDiv") as HTMLDivElement;
    const newNote = document.createElement("div") as HTMLDivElement;

    
}